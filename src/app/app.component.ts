import { Component } from '@angular/core';

@Component({
  selector: 'app-root', // Selector del componente
  templateUrl: './app.component.html', // Ruta del archivo HTML del componente
  styleUrls: ['./app.component.css'] // Rutas de los archivos CSS del componente
})
export class AppComponent {
  title = 'FERNANDO APONTE'; // Propiedad del componente para el título
}
